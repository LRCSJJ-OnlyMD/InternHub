# PostgreSQL Database Configuration (Neon - Serverless PostgreSQL)
# Sign up at: https://console.neon.tech
# Format: jdbc:postgresql://<endpoint>.neon.tech:5432/<database>?sslmode=require
DB_URL=jdbc:postgresql://ep-example-123.neon.tech:5432/neondb?sslmode=require
DB_USERNAME=your-neon-username
DB_PASSWORD=your-neon-password

# Azure Cache for Redis (Optional - only if you add Redis)
REDIS_HOST=internhub-cache.redis.cache.windows.net
REDIS_PORT=6380
REDIS_PASSWORD=your-redis-access-key

# JWT Secret (IMPORTANT: Generate a secure random 256-bit key)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=change-this-to-a-very-long-secure-random-string-at-least-256-bits

# Email Configuration for Gmail SMTP
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Note: For Gmail, you need to:
# 1. Enable 2-step verification on your Google account
# 2. Generate an App Password (https://myaccount.google.com/apppasswords)
# 3. Use the generated App Password here (not your regular Gmail password)

# Frontend URL (for CORS configuration in backend)
FRONTEND_URL=https://internhub.azurewebsites.net

# Backend API URL (for frontend configuration)
API_URL=https://internhub-backend.azurewebsites.net/api

